import * as React from 'react';
interface IProps {
    children?: React.ReactNode;
    open?: boolean;
}
export declare class RightPanel extends React.PureComponent<IProps> {
    render(): JSX.Element;
}
export {};
