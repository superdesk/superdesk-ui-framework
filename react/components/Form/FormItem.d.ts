import * as React from 'react';
interface IProps {
    children: React.ReactNode;
    autoWidth?: boolean;
}
export declare class FormItem extends React.PureComponent<IProps> {
    render(): JSX.Element;
}
export {};
